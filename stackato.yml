name: scheduler
framework:
  type: python
mem: 128M
requirements:
  pypm:
    - bottle
    - redis
min_version:
  client: 1.4.3
services:
  cache-db: redis
env:
# RESERVATION_URL: localhost
  RESERVATION_URL: http://scheduler.acmehealthco.com/